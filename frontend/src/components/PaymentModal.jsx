import React from 'react'

const PaymentModal = () => {
	return (
		<div className="container py-4" role="status" aria-live="polite">
			<div className="text-muted">Payment modal placeholder. Use PaymentPage routes.</div>
		</div>
	)
}

export default PaymentModal
