import React from 'react'

// Placeholder component to avoid broken imports; real editor lives in EditProposal and CreateProposalForm flow.
const ProposalEditor = () => {
	return (
		<div className="container py-4" role="status" aria-live="polite">
			<div className="text-muted">Proposal editor is handled by the Create and Edit flows.</div>
		</div>
	)
}

export default ProposalEditor
