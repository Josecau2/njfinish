# UI Audit Workflow

name: UI Audit

on:
  push:
    branches:
      - njnewui

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install dependencies
      run: npm install

    - name: Run UI audit
      run: npm run ui-audit